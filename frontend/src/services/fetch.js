import { useEffect, useState } from "react"
import { useFetch } from "../Hooks/useFetch.js"

export default function LoginFetch({url}){
    const [valeur, setvaleur] = useState()
    useEffect(()=>{
        async function fetchData(){

        }
        fetchData()
    },[])
}